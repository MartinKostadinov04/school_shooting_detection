# pipeline package
# Modules: preprocessing, extract_embeddings, split_dataset
#
# NOTE: pipeline/config.py contains mel spectrogram parameters for the
# separate CNN experiment. It is NOT used by the YAMNet pipeline.
# The YAMNet pipeline is configured via configs/yamnet_pipeline.yaml.
