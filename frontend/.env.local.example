# Frontend environment variables — copy to .env.local (gitignored)
# These are picked up by Vite at build time.

# Leave blank — the frontend fetches a token from /api/ably-token
VITE_ABLY_API_KEY=

# FastAPI backend base URL (change in production)
VITE_API_BASE_URL=http://localhost:8000
