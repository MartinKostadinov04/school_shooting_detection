# models package
# Contains classification head architectures and the two-stage cascade gate.
#
# Current stubs (to be implemented in future sessions):
#   head_dense.py   — Dense MLP head trained on YAMNet embeddings
#   head_bilstm.py  — BiLSTM head trained on YAMNet embeddings
#   cascade/gate.py — YAMNet class-427 score gate for the two-stage system
