# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Data — raw audio files, processed embeddings, splits, SQLite DB
data/

# Trained model weights
# models/saved_weights/ is NOT ignored — dense_head_best.keras is tracked via LFS
models/yolo_finetuned/last.pt  # large duplicate of best.pt — not needed in repo

# Experiment run logs (JSON/CSV artefacts — not source)
experiments/runs/*.json
experiments/runs/*.csv

# Inference detection logs (runtime output, not source)
inference/detections.jsonl
inference/cascade_detections.jsonl
vision/detections.jsonl
vision/cascade_detections.jsonl

# TensorFlow Hub cache
/tmp/tfhub_modules/
~/.cache/tfhub_modules/

# OS
.DS_Store
Thumbs.db

# Modal — never commit credentials
.modal.toml
modal.toml

# IDE
.vscode/
.idea/
*.swp

# Frontend
frontend/node_modules/
frontend/.env
frontend/.env.local
frontend/dist/
frontend/.output/

# Secrets
.env
